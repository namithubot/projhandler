<h3>Add him/her? Are you sure? </h3>
<%= form_for :group, url: member_path(@group), :class => "ui form" , method: :patch do |f| %>
	<%= f.label :user %>
   <%= f.text_field :user_name, :readonly => true %><br>
	<%= f.label :in_group %>
  <%= f.text_field :group, :readoly =>true %><br> 
<p>
	 <%= f.submit %>
 </p>
<% end %>
<%= link_to 'Cancel', controller: 'add_project' %>
